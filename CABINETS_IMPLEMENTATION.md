# Реализованный функционал личных кабинетов

## ✅ Что реализовано:

### 1. Личный кабинет LEADER (Руководитель бригады)

**Доступ:** Кнопка "👔 Личный кабинет" в главном меню

**Функционал:**
- ✅ **Просмотр подчиненных** (`leader_view_workers`)
  - Список всех работников (WORKER) своей бригады
  - Статус каждого работника (доступен/недоступен)
  - Telegram ID работников
  - Информация о бригаде (part_name)

- ✅ **Просмотр ошибок бригады** (`leader_view_errors`)
  - Список всех ошибок, допущенных бригадой
  - Дата каждой ошибки
  - Описание ошибки
  - Наличие фото к ошибке
  - Показывает последние 10 ошибок

**Файлы:**
- `app/handlers/leader.py` - обработчики
- `app/keyboards/leader_keyboards.py` - клавиатуры
- `app/states/leader_states.py` - FSM состояния

### 2. Расширенный функционал MANAGER

**Дополнительные возможности менеджера (в разработке):**
- 📅 **Планирование проверок** (`manager_plan_checks`)
  - Выбор бригады для проверки
  - Выбор даты (по умолчанию завтра)
  - Выбор времени проверки
  - Назначение проверяющего (office_worker)
  - Управление задачами в planned_checks

**Файлы:**
- `app/keyboards/manager_keyboards.py` - клавиатуры для менеджера
- `app/states/manager_states.py` - FSM состояния
- `app/services/plannedCheckService.py` - сервис для работы с запланированными проверками

### 3. Расширенный функционал ADMIN

**Возможности администратора (в разработке):**
- 👥 **Управление пользователями**
  - Создание новых пользователей
  - Изменение уровней доступа
  - Назначение в бригады
  - Удаление пользователей

**Файлы:**
- `app/keyboards/admin_keyboards.py` - клавиатуры для администратора
- `app/states/admin_states.py` - FSM состояния

## 📊 Обновленные сервисы:

### UserService
Новые методы:
- `get_workers_by_part_name(part_name)` - получить работников бригады
- `get_leaders()` - получить всех руководителей
- `get_leader_by_part_name(part_name)` - получить руководителя бригады
- `get_all_users()` - получить всех пользователей
- `update_user_access_level(name, access_level)` - изменить уровень доступа
- `assign_worker_to_brigade(worker_name, part_name)` - назначить в бригаду
- `get_office_workers()` - получить всех офисных работников

### CheckService
Новые методы:
- `get_checks_by_form(form_id)` - получить проверки по форме
- `get_errors_by_form(form_id)` - получить ошибки бригады

### PlannedCheckService (новый)
- `create_planned_check()` - создать запланированную проверку
- `get_all_planned_checks()` - получить все запланированные проверки
- `get_planned_checks_by_form()` - проверки по форме
- `get_planned_checks_by_reviewer()` - проверки по проверяющему
- `update_planned_check_time()` - изменить время проверки
- `update_planned_check_reviewer()` - изменить проверяющего
- `delete_planned_check()` - удалить проверку
- `get_upcoming_checks()` - ближайшие проверки

## 🔄 Обновленные компоненты:

### main_menu.py
- Добавлена кнопка "👔 Личный кабинет" для LEADER
- Разделены интерфейсы для LEADER и WORKER

### main.py
- Зарегистрирован роутер `leader`
- Инициализирован `PlannedCheckService`

## 🚧 В разработке:

### Для MANAGER:
1. Полная реализация планирования проверок
2. Интерфейс выбора задач для planned_checks
3. Редактирование запланированных проверок

### Для ADMIN:
1. Полный CRUD пользователей
2. Интерфейс назначения в бригады
3. Массовое управление пользователями

## 📝 Структура базы данных:

### Таблица users
```sql
CREATE TABLE users (
    id TEXT,                    -- Telegram ID
    name TEXT PRIMARY KEY,      -- ФИО
    access_level TEXT,          -- Уровень доступа
    available BOOL,             -- Доступность
    part_name TEXT              -- Бригада (для LEADER и WORKER)
)
```

### Таблица planned_checks
```sql
CREATE TABLE planned_checks (
    id INTEGER PRIMARY KEY,
    time DATETIME,              -- Время проверки
    form_id INTEGER,            -- ID формы (бригады)
    reviewer_id INTEGER,        -- ID проверяющего
    created_at DATETIME
)
```

## 🎯 Как использовать:

### Для руководителя бригады (LEADER):
1. Войти в бот
2. Нажать "👔 Личный кабинет"
3. Выбрать "👥 Мои подчиненные" или "⚠️ Ошибки бригады"

### Для менеджера (MANAGER):
1. Войти в "🏢 Рабочий кабинет"
2. Управление формами, задачами
3. Планирование проверок (в разработке)

### Для администратора (ADMIN):
1. Войти в "🏢 Рабочий кабинет"
2. Управление пользователями (в разработке)
3. Полный доступ ко всем функциям

## ⚙️ Требования:

- Python 3.13+
- aiogram 3.x
- aiosqlite
- Настроенная база данных SQLite

## 🔗 Связи в системе:

```
ADMIN
  └─ Управляет всеми пользователями
  └─ Назначает уровни доступа

MANAGER  
  └─ Создает формы (бригады)
  └─ Планирует проверки
  └─ Назначает проверяющих

LEADER
  └─ Управляет бригадой (part_name)
  └─ Видит своих WORKER'ов
  └─ Отслеживает ошибки бригады

OFFICE_WORKER
  └─ Проводит проверки
  └─ Назначается менеджером

WORKER
  └─ Входит в состав бригады
  └─ Имеет part_name
```
